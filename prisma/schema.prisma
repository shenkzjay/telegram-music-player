// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")

  
}



model User {
  id        String   @id @default(uuid()) 
  tgId      String   @unique // Telegram User ID
  username  String?
  channels  Channel?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Channel {
  id        String   @id @default(uuid())
  tgChatId    String   @unique // Telegram Channel ID (e.g., -100123...)
  userId    String   @unique // Owner's Telegram ID
  user      User     @relation(fields: [userId], references: [id])
  songs     Song[]
  isVerified Boolean @default(false)  // True when channel is verified 
  isActive Boolean @default(true) // True when bot is active
}

model Song {
  id        Int      @id @default(autoincrement())
  fileId    String  @unique // Telegram File ID
  title     String
  artist    String
  duration  Int // Duration in seconds
  channelId String
  channel   Channel  @relation(fields: [channelId], references: [id])
}